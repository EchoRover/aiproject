# Models Folder

Save trained models here.

## Structure:
- Save PyTorch models as `.pt` or `.pth` files
- Include model architecture definitions if needed
- Keep track of model versions and performance

Example:
```
models/
├── neural_net_v1.pth
├── best_model.pt
└── model_architectures.py
```
